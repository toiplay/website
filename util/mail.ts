const email: string = typeof window !== 'undefined' ?
    ' '.concat('wxizirDoveljsvwx2hiz').split('').reduce((prev: string, char: string): string => prev + String.fromCharCode(char.charCodeAt(0) - 4)).substring(1) :
    '{{HIDDEN}}';

export default email;