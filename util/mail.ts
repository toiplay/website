const email: string = typeof window !== 'undefined' ?
    ' '.concat('wxizirkimpssDsyxpsso2hi').split('').reduce((prev: string, char: string): string => prev + String.fromCharCode(char.charCodeAt(0) - 4)).substring(1) :
    '{{HIDDEN}}';

export default email;